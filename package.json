{
  "name": "tarot-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest --run"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.0",
    "marked": "^12.0.0",
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "fast-check": "^3.0.0",
    "vitest": "^1.0.0"
  }
}
